# Architecture

The full architecture documentation lives at
[`../ARCHITECTURE.md`](../ARCHITECTURE.md) at the repository root — this
file exists so `docs/` has a complete index, per the documentation
structure convention used across CeloHT repositories.
